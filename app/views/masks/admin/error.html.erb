<div class="px-2 mb-2">
  <div class="flex items-center gap-5">
    <div class="flex-grow">
      <h1 class="text-2xl md:text-3xl font-bold text-error">
        <%= t(".openid.#{params[:error]}") %>
      </h1>
      <h2 class="text-xl">
        <%= t(".openid_desc.#{params[:error]}") || params[:error_description] %>
      </h2>
    </div>
    <a class="btn btn-outline" href="<%= session_path(prompt: 'select_account') %>" data-turbo="false">
      try again
    </a>
  </div>
</div>

